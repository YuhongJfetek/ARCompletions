@model IEnumerable<ARCompletions.Domain.VendorStaffUser>

<h2>廠商人員</h2>

<p><a asp-area="Admin" asp-controller="VendorStaffUsers" asp-action="Create">新增</a></p>

<table class="table">
    <thead>
        <tr>
            <th>LineUserId</th>
            <th>Name</th>
            <th>Role</th>
            <th>IsActive</th>
            <th>CreatedAt</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach(var item in Model) {
        <tr>
            <td>@item.LineUserId</td>
            <td>@item.Name</td>
            <td>@item.Role</td>
            <td>@item.IsActive</td>
            <td>@item.CreatedAt</td>
            <td>
                <a asp-area="Admin" asp-controller="VendorStaffUsers" asp-action="Details" asp-route-id="@item.Id">檢視</a> |
                <a asp-area="Admin" asp-controller="VendorStaffUsers" asp-action="Edit" asp-route-id="@item.Id">編輯</a> |
                <a asp-area="Admin" asp-controller="VendorStaffUsers" asp-action="Delete" asp-route-id="@item.Id">刪除</a>
            </td>
        </tr>
    }
    </tbody>
</table>
