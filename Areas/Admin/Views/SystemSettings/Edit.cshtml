@model ARCompletions.Domain.SystemSetting
@{
    ViewData["Title"] = "Edit System Setting";
}

<h1>編輯設定</h1>

<form asp-action="Edit" method="post">
    <input type="hidden" name="id" value="@Model.Id" />
    <div class="mb-3">
        <label class="form-label">鍵</label>
        <input class="form-control" value="@Model.SettingKey" disabled />
    </div>
    <div class="mb-3">
        <label class="form-label">值</label>
        <input name="SettingValue" class="form-control" value="@Model.SettingValue" />
    </div>
    <div class="mb-3">
        <label class="form-label">描述</label>
        <textarea name="Description" class="form-control">@Model.Description</textarea>
    </div>
    <button class="btn btn-primary">儲存</button>
    <a asp-area="Admin" asp-controller="SystemSettings" asp-action="Index" class="btn btn-secondary">取消</a>
</form>
