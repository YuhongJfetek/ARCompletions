@model ARCompletions.Areas.Vendor.Controllers.VendorLoginViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "廠商登入";
    var returnUrl = ViewData["ReturnUrl"] as string;
}
<div class="login-wrap">
    <div class="login-card">
        <div class="brand">廠商登入</div>
        <form asp-action="Login" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" name="returnUrl" value="@returnUrl" />

            <div class="form-row">
                <label asp-for="Email">Email</label>
                <input id="Email" name="Email" type="email" value="@(Model?.Email ?? string.Empty)" />
                <span asp-validation-for="Email" class="validation"></span>
            </div>

            <div class="form-row">
                <label asp-for="Password">密碼</label>
                <input id="Password" name="Password" type="password" value="" />
                <span asp-validation-for="Password" class="validation"></span>
            </div>

            <button type="submit" class="btn-primary">登入</button>
        </form>

        @if (!ViewData.ModelState.IsValid)
        {
            <div class="validation">@string.Join("; ", ViewData.ModelState.Values.SelectMany(v => v.Errors).Select(e => e.ErrorMessage))</div>
        }

        <div class="helper">請使用註冊的 Email 與密碼登入</div>
    </div>
</div>
